[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: Unit tests for individual modules
    integration: Integration tests requiring ROS 2
    determinism: Tests verifying deterministic behavior
